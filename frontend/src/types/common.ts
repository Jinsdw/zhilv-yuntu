/** 通用基础类型 */

/** 日期字符串 (YYYY-MM-DD)，对应后端 date 序列化 */
export type ISODate = string

/** 时间字符串 (HH:mm) */
export type ClockTime = string

/** 日期时间字符串 (ISO 8601)，对应后端 datetime 序列化 */
export type ISODateTime = string