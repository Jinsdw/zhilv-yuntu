# 开发环境变量（Vite 约定：只有 VITE_ 前缀会注入客户端）
VITE_APP_TITLE=智旅云图
# API 基础路径：留空表示同源请求，由 vite dev server 代理转发到后端
VITE_API_BASE_URL=
# vite dev 代理目标（后端 FastAPI 默认端口）
VITE_API_PROXY_TARGET=http://localhost:8000
# 前端开发端口
VITE_DEV_PORT=5173